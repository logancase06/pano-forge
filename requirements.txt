# pano-forge dependencies

# Image I/O, EXIF handling
Pillow>=10.2.0

# Numerical / array ops (used by panorama stitching & forge stages)
numpy>=1.26.0

# Panorama generation (DiT360) and photo->prompt captioning (Qwen-VL),
# both via hosted Hugging Face Gradio Spaces (free, no GPU, no API key)
gradio_client>=1.3.0

# Local DiT360 backend (optional, GPU/CUDA) — uncomment to run the model locally
# diffusers>=0.30.0
# torch>=2.2.0
# transformers>=4.44.0
# accelerate>=0.33.0

# Tests
pytest>=8.0.0
